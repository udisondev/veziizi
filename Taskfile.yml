version: '3'

dotenv: ['.env.dev']

vars:
  POSTGRES_USER: '{{.POSTGRES_USER | default "veziizi"}}'
  POSTGRES_PASSWORD: '{{.POSTGRES_PASSWORD | default "veziizi"}}'
  POSTGRES_DB: '{{.POSTGRES_DB | default "veziizi"}}'
  POSTGRES_HOST: '{{.POSTGRES_HOST | default "localhost"}}'
  POSTGRES_PORT: '{{.POSTGRES_PORT | default "5432"}}'
  DATABASE_URL: 'postgres://{{.POSTGRES_USER}}:{{.POSTGRES_PASSWORD}}@{{.POSTGRES_HOST}}:{{.POSTGRES_PORT}}/{{.POSTGRES_DB}}?sslmode=disable'
  TEST_DATABASE_URL: 'postgres://{{.POSTGRES_USER}}:{{.POSTGRES_PASSWORD}}@{{.POSTGRES_HOST}}:{{.POSTGRES_PORT}}/veziizi_test?sslmode=disable'
  WORKERS: members invitations pending-organizations organizations freight-requests review-receiver review-analyzer reviews-projection review-activator fraudster-handler notification-dispatcher telegram-sender email-sender support-tickets rate-limiter-cleanup
  COMPOSE_DEV: '-f docker-compose.yml -f docker-compose.dev.yml'
  COMPOSE_STAGING: '-f docker-compose.yml -f docker-compose.staging.yml'
  COMPOSE_PROD: '-f docker-compose.yml -f docker-compose.prod.yml'

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list-all

  # --- Docker (dev Postgres) ---

  up:
    desc: Start dev Postgres
    cmds:
      - docker compose {{.COMPOSE_DEV}} up -d

  down:
    desc: Stop dev Postgres
    cmds:
      - docker compose {{.COMPOSE_DEV}} down

  logs:
    desc: Show dev logs
    interactive: true
    cmds:
      - docker compose {{.COMPOSE_DEV}} logs -f {{.CLI_ARGS}}

  db-shell:
    desc: Connect to PostgreSQL shell
    interactive: true
    cmds:
      - docker compose {{.COMPOSE_DEV}} exec postgres psql -U {{.POSTGRES_USER}} -d {{.POSTGRES_DB}}

  # --- Migrations ---

  migrate:
    desc: Run migrations up
    cmds:
      - goose -dir backend/migrations postgres "{{.DATABASE_URL}}" up

  migrate:down:
    desc: Run migrations down (one step)
    cmds:
      - goose -dir backend/migrations postgres "{{.DATABASE_URL}}" down

  migrate:status:
    desc: Show migration status
    cmds:
      - goose -dir backend/migrations postgres "{{.DATABASE_URL}}" status

  migrate:create:
    desc: Create new migration (use -- migration_name)
    preconditions:
      - sh: '[ -n "{{.CLI_ARGS}}" ]'
        msg: "Usage: task migrate:create -- migration_name"
    cmds:
      - goose -dir backend/migrations create {{.CLI_ARGS}} sql

  # --- Build ---

  build:
    desc: Build all binaries
    deps: [build:api, build:telegram-bot, build:workers]

  build:api:
    desc: Build API server
    cmds:
      - go build -o bin/api ./backend/cmd/api
    sources:
      - backend/**/*.go
      - go.mod
      - go.sum
    generates:
      - bin/api

  build:telegram-bot:
    desc: Build Telegram bot
    cmds:
      - go build -o bin/telegram-bot ./backend/cmd/telegram-bot
    sources:
      - backend/**/*.go
      - go.mod
      - go.sum
    generates:
      - bin/telegram-bot

  build:workers:
    desc: Build all workers
    cmds:
      - for: {var: WORKERS}
        cmd: go build -o bin/worker-{{.ITEM}} ./backend/cmd/workers/{{.ITEM}}

  # --- Run ---

  run:api:
    desc: Run API server
    cmds:
      - go run ./backend/cmd/api

  run:telegram-bot:
    desc: Run Telegram bot for link code handling
    cmds:
      - go run ./backend/cmd/telegram-bot

  run:workers:
    desc: Run all workers in background
    cmds:
      - |
        for worker in {{.WORKERS}}; do
          go run ./backend/cmd/workers/$worker &
        done
        echo "All workers started in background"

  # --- Testing ---

  test:
    desc: Run unit tests
    cmds:
      - go test -v ./backend/internal/...

  test:cover:
    desc: Run tests with coverage
    cmds:
      - go test -v -coverprofile=coverage.out ./backend/internal/...
      - go tool cover -html=coverage.out -o coverage.html

  test:e2e:setup:
    internal: true
    deps: [up]
    cmds:
      - echo "Creating test database..."
      - docker exec veziizi-postgres psql -U {{.POSTGRES_USER}} -c "DROP DATABASE IF EXISTS veziizi_test" || true
      - docker exec veziizi-postgres psql -U {{.POSTGRES_USER}} -c "CREATE DATABASE veziizi_test"
      - echo "Running migrations on test database..."
      - goose -dir backend/migrations postgres "{{.TEST_DATABASE_URL}}" up
      - echo "Seeding geo data..."
      - TEST_DATABASE_URL="{{.TEST_DATABASE_URL}}" go run ./backend/cmd/tools/seed-geo
      - echo "Creating test admin..."
      - DATABASE_URL="{{.TEST_DATABASE_URL}}" go run ./backend/cmd/tools/create-admin --email="admin@veziizi.local" --name="Admin" --password="admin123" || true
      - echo "E2E test database ready"

  test:e2e:
    desc: Run E2E tests (uses docker-compose DB)
    deps: [test:e2e:setup]
    cmds:
      - TEST_DATABASE_URL="{{.TEST_DATABASE_URL}}" go test -v -count=1 -p=1 ./backend/e2e/tests/...

  test:e2e:parallel:
    desc: Run E2E tests in parallel (uses docker-compose DB)
    deps: [test:e2e:setup]
    cmds:
      - TEST_DATABASE_URL="{{.TEST_DATABASE_URL}}" go test -v -count=1 ./backend/e2e/tests/...

  test:e2e:containers:
    desc: Run E2E tests with testcontainers (requires Docker)
    cmds:
      - go test -v -count=1 -p=1 ./backend/e2e/tests/...

  # --- Code Quality ---

  lint:
    desc: Run linter
    cmds:
      - golangci-lint run ./...

  fmt:
    desc: Format code
    cmds:
      - go fmt ./...
      - goimports -w .

  tidy:
    desc: Tidy go modules
    cmds:
      - go mod tidy

  generate:
    desc: Run go generate
    cmds:
      - go generate ./...

  # --- Development ---

  dev:setup:
    internal: true
    cmds:
      - command -v goreman >/dev/null 2>&1 || go install github.com/mattn/goreman@latest
      - command -v air >/dev/null 2>&1 || go install github.com/air-verse/air@latest

  dev:
    desc: Start DB + API only (without workers/frontend)
    cmds:
      - task: up
      - task: migrate
      - task: seed:geo
      - task: run:api

  dev:all:
    desc: Start everything with hot-reload (API + workers + frontend)
    cmds:
      - task: dev:setup
      - task: up
      - task: build:workers
      - echo "Waiting for PostgreSQL..."
      - until docker exec veziizi-postgres pg_isready -U {{.POSTGRES_USER}} >/dev/null 2>&1; do sleep 1; done
      - echo "PostgreSQL ready, running migrations..."
      - task: migrate
      - echo "Seeding geo data..."
      - task: seed:geo
      - echo "Starting all services..."
      - goreman -f Procfile.dev start

  dev:docker:
    desc: Full stack in Docker (no hot-reload)
    cmds:
      - docker compose {{.COMPOSE_DEV}} --profile docker-dev build
      - docker compose {{.COMPOSE_DEV}} --profile docker-dev up -d
      - echo "Waiting for PostgreSQL..."
      - until docker compose {{.COMPOSE_DEV}} exec -T postgres pg_isready -U veziizi >/dev/null 2>&1; do sleep 1; done
      - echo "Running migrations..."
      - task: migrate
      - echo "Seeding geo data..."
      - task: seed:geo
      - echo "=== Dev stack running in Docker ==="
      - docker compose {{.COMPOSE_DEV}} --profile docker-dev ps

  dev:docker:down:
    desc: Stop full Docker dev stack
    cmds:
      - docker compose {{.COMPOSE_DEV}} --profile docker-dev down

  back:dev:
    desc: Run backend with air (hot-reload)
    cmds:
      - air

  # --- Staging ---

  stage:
    desc: Build and start all staging services
    preconditions:
      - sh: test -f .env.secrets
        msg: ".env.secrets not found. Copy .env.secrets.example and fill in values."
    cmds:
      - docker compose {{.COMPOSE_STAGING}} build
      - docker compose {{.COMPOSE_STAGING}} up -d
      - echo "Waiting for PostgreSQL..."
      - until docker compose {{.COMPOSE_STAGING}} exec -T postgres pg_isready -U veziizi >/dev/null 2>&1; do sleep 1; done
      - echo "Running migrations..."
      - docker compose {{.COMPOSE_STAGING}} run --rm api migrator up
      - echo "=== Staging is running ==="
      - docker compose {{.COMPOSE_STAGING}} ps

  stage:down:
    desc: Stop staging services
    cmds:
      - docker compose {{.COMPOSE_STAGING}} down

  stage:logs:
    desc: Show staging logs (use -- service-name for specific)
    interactive: true
    cmds:
      - docker compose {{.COMPOSE_STAGING}} logs -f {{.CLI_ARGS}}

  stage:rebuild:
    desc: Rebuild and restart specific service (use -- service-name)
    preconditions:
      - sh: '[ -n "{{.CLI_ARGS}}" ]'
        msg: "Usage: task stage:rebuild -- api  or  task stage:rebuild -- worker-members"
    cmds:
      - docker compose {{.COMPOSE_STAGING}} build {{.CLI_ARGS}}
      - docker compose {{.COMPOSE_STAGING}} up -d --no-deps {{.CLI_ARGS}}

  stage:rebuild-all:
    desc: Rebuild and restart all services
    cmds:
      - docker compose {{.COMPOSE_STAGING}} up -d --build

  stage:ps:
    desc: Show staging services status
    cmds:
      - docker compose {{.COMPOSE_STAGING}} ps

  # --- Production ---

  prod:
    desc: Build and start production
    preconditions:
      - sh: test -f .env.secrets
        msg: ".env.secrets not found."
    cmds:
      - docker compose {{.COMPOSE_PROD}} build
      - docker compose {{.COMPOSE_PROD}} up -d
      - echo "Waiting for PostgreSQL..."
      - until docker compose {{.COMPOSE_PROD}} exec -T postgres pg_isready -U veziizi >/dev/null 2>&1; do sleep 1; done
      - echo "=== Production is running ==="
      - docker compose {{.COMPOSE_PROD}} ps

  prod:down:
    desc: Stop production services
    cmds:
      - docker compose {{.COMPOSE_PROD}} down

  prod:logs:
    desc: Show production logs
    interactive: true
    cmds:
      - docker compose {{.COMPOSE_PROD}} logs -f {{.CLI_ARGS}}

  prod:ps:
    desc: Show production services status
    cmds:
      - docker compose {{.COMPOSE_PROD}} ps

  # --- Admin Management ---

  create:admin:
    desc: Create platform admin (interactive)
    interactive: true
    cmds:
      - |
        read -p "Email: " email
        read -p "Name: " name
        read -s -p "Password: " password
        echo
        go run ./backend/cmd/tools/create-admin \
          --email="$email" \
          --name="$name" \
          --password="$password"

  create:admin:dev:
    desc: Create dev admin (admin@veziizi.local / admin123)
    cmds:
      - go run ./backend/cmd/tools/create-admin --email="admin@veziizi.local" --name="Admin" --password="admin123"

  create:test-org:
    desc: Create test org with owner (owner@test.local / test123)
    cmds:
      - >-
        go run ./backend/cmd/tools/create-test-org
        --email="owner@test.local"
        --password="test123"
        --name="Test Owner"
        --org="Test Organization"
        --approve=true

  # --- Seed & Tools ---

  seed:geo:
    desc: Seed geo data (countries and cities)
    cmds:
      - go run ./backend/cmd/tools/seed-geo

  seed:orgs:
    desc: Seed test organizations
    cmds:
      - go run ./backend/cmd/tools/seed-orgs

  resend:telegram:
    desc: Resend failed telegram notifications
    cmds:
      - go run ./backend/cmd/tools/resend-telegram

  backfill:freight-requests:
    desc: Backfill freight requests projection
    cmds:
      - go run ./backend/cmd/tools/backfill-freight-requests
